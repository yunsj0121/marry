# AGENTS.md

삼성카드 사내 웨딩홀 예약 사이트(`s-wedding.samsungcard.com`)를 Playwright로 자동화하는
저장소입니다. 실제 운영 중이며, 실패하면 실제로 예약 기회를 놓칩니다.

## 두 개의 진입점

| 명령 | 워크플로우 | 역할 |
|---|---|---|
| `marry-monitor` | `wedding-seat-monitor.yml` | 취소표 모니터링. 상태만 읽고 **신청하지 않음**. 새로 열린 자리가 생기면 텔레그램 알림 |
| `marry-book` | `wedding-hall-booking.yml` | 오픈 순간 실제 신청. `workflow_dispatch` 전용 |

모니터는 GitHub `schedule`(KST 7~22시, 매시 7/27/47분)과 외부 cron-job.org의
`workflow_dispatch` 호출(15분 간격)로 함께 돌고 있습니다.

`marry-book`에 `schedule` 트리거를 **절대 추가하지 마세요**. 실제 신청까지 자동 진행되는
민감한 동작이라, 오픈 시각보다 충분히 앞서 외부에서 명시적으로 트리거하는 방식만 씁니다.
실제 클릭 타이밍은 스크립트 내부 `wait_until()`이 시스템 시계로 맞춥니다.

## 작업 원칙 (가장 중요)

이 저장소의 버그는 거의 전부 **사이트 쪽 렌더 지연/비동기 재렌더**입니다. 코드 결함이 아니라
타이밍 문제라서, 추측으로 고치면 틀립니다. 실제로 한 번 틀린 진단으로 고쳤다가
(PR #13) 다음 실패 때 같은 증상이 재현된 적이 있습니다.

1. **실제 로그로 진단하세요. 추측 금지.** 실패 알림에는 GitHub Actions 실행 링크가 있습니다.
   해당 run의 job 로그를 읽고, 마지막으로 성공한 단계와 실패 지점을 특정하세요.
2. **Playwright 콜로그를 끝까지 읽으세요.** `waiting for locator(...)`에서 끝나고
   `locator resolved to ...`가 없으면 → 요소가 **DOM에 없었던 것**(가려진 게 아님).
   `locator resolved to` 이후 줄이 있으면 → 요소는 있었고 actionability에서 막힌 것.
   이 둘은 원인도 해법도 완전히 다릅니다.
3. **고립된 사고인지 확인하세요.** 실패한 run 앞뒤 실행들의 conclusion을 보세요.
   앞뒤가 전부 성공이면 사이트 쪽 일시적 지연입니다. 이 경우에도 해당 지점에 재시도가
   없다면 추가할 가치가 있지만, 추가 방어 로직을 끝없이 쌓지는 마세요.
4. **최소한으로 고치세요.** 증상이 나타난 그 지점만. 주변 정리나 리팩터링을 끼워 넣지 마세요.
5. **회귀 테스트를 추가하세요.** 실제 사이트 없이 도는 오프라인 테스트여야 합니다.
6. 수정의 근거가 된 run 번호를 코드 주석과 커밋 메시지에 남기세요. 다음 사람이
   "이 방어 로직이 왜 있는지" 추적할 수 있어야 합니다.

## 이미 확인된 사이트 특이점

같은 함정에 다시 빠지지 마세요. 전부 실제 실패에서 확인된 것입니다.

- **로그인 세션이 약 10분**입니다. 오픈 30분 전에 로그인해두면 정작 오픈 순간에 끊깁니다.
  `marry-book`은 `LOGIN_LEAD_SECONDS`만큼만 앞서 로그인합니다.
- **오픈 정각에 바로 안 열립니다.** 실제로 몇 초 뒤에 다음 달로 넘어갈 수 있어,
  `wait_for_month_open()`이 `MONTH_OPEN_RETRY_SECONDS` 동안 재시도합니다.
- **홀을 바꾸면 달력 데이터를 비동기로 다시 불러옵니다.** 그 사이 달력이 통째로 다시
  그려지며 표시 월이 되돌아가, 방금 `visible=True`로 확인한 날짜 버튼이 클릭 시점에
  DOM에서 사라집니다(run #2831). 같은 locator를 다시 누르면 소용없고, 목표 월 이동부터
  다시 해야 합니다.
- **달력에 "안내" 팝업이 떠서 다음달 버튼을 가립니다.** `close_calendar_notice()`로 닫고
  재시도합니다.
- **정보입력 화면의 "구분" 라디오 onchange가 다른 입력값을 초기화합니다.** 신랑/신부 성명뿐
  아니라 생년월일·휴대전화번호까지 지워집니다. 반드시 **구분을 먼저 선택한 뒤** 나머지를
  채우세요.
- **렌더가 늦는 구간이 많습니다**: 회사 검색란, 회사 검색 결과, "선택 완료" 이후 화면,
  보안프로그램 선택 라디오, 로그인 제출 후 화면 전환. 전부 폴링/재시도로 방어돼 있습니다.
  새로 고정 대기(`wait_for_timeout`) 하나만 두고 바로 실패 판정하는 코드를 만들지 마세요.
- **삼성금융연수원은 구형 `<table>` 달력**이라 `data-date` 버튼이 없습니다.
  `click_table_day()`로 처리하며, 마감 날짜는 클릭 없이 판정합니다.
- 클릭 시간 초과를 **"예약마감"으로 취급하지 마세요.** 확인불가(unknown) 또는 실행 실패로
  처리해야 실제로 열린 자리를 놓치지 않습니다.

## 안전 규칙

- **신청 버튼은 단 한 번만 누릅니다.** 클릭 이후의 모든 처리는 지망 순회 `try/except` **밖**에
  있어야 합니다. 클릭 후 오류가 나면 중복 신청 위험이 있으므로 다음 지망으로 넘어가지 않고
  결과 불명으로 종료합니다. 이 구조를 바꾸지 마세요.
- 필수 개인정보 7개 항목이 모두 정상 입력된 경우에만 제출합니다.
- 개인정보는 전부 GitHub Secrets에 있습니다. 코드·로그·커밋에 실제 값을 남기지 마세요.
  저장소는 **Public**입니다(Actions 분 무제한 목적).
- 모니터는 어떤 경우에도 신청하지 않습니다.

## 개발

```bash
pip install -e ".[test]"
python -m pytest -q          # 실제 로그인/신청/텔레그램 전송 없음
```

테스트가 통과해야 `marry-book`이 실행됩니다(워크플로우에 테스트 단계 포함).

### 브랜치 / PR

- `main`에서 최신을 받아 작업 브랜치를 시작하세요.
  `main`에는 모니터가 `state/availability.json`을 자동 커밋하므로 자주 갱신됩니다.
- PR을 만들고 **squash merge** 합니다.
- PR 본문에는 근거가 된 run 번호와 로그에서 읽은 실패 지점을 적으세요.

## 모니터링 타깃 관리

`src/marry/monitor.py` 상단:

- `TARGETS` — 고정 타깃. `Target(year, month, day, time, hall)`이며 `hall` 기본값은 서초사옥.
  지정한 날짜·시간만 정확히 확인합니다. `saturday_targets()`로 특정 월의 토요일을 일괄 생성합니다.
- `HALL_DAY_SCANS` — 특정 하루의 모든 시간대를 훑습니다.
- `HALL_MONTH_SCANS` — 한 달 전체에서 마감이 아닌 날짜만 훑습니다. 고정 타깃보다 느립니다.

현재 고정 타깃은 서초사옥 31개 + 삼성금융연수원 27개(2027년 10·11월 토요일 11:00/13:00/15:00 포함)입니다.

> 타깃을 늘리면 실행 시간이 늘어납니다. 현재 한 번 실행에 약 6분이고 로그인 세션은 약 10분이라
> 여유가 많지 않습니다. 크게 늘릴 때는 세션 만료를 함께 고려하세요.
