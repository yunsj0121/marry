# 취소표 모니터 설정

이 기능은 GitHub Actions에서 한국 시간 기준 매일 7시~22시 사이 30분마다(정각 7분, 37분) 실행되어 다음 예약 상태를 확인합니다. 23시~다음날 7시 사이에는 실행하지 않습니다. 자동 예약이나 신청은 하지 않습니다.

**서초사옥** (정해진 날짜/시간대만 확인)

- 2027-08-28 17:00
- 2027-09-04 11:00
- 2027-09-04 13:00
- 2027-09-04 17:00

날짜/시간대를 추가하거나 빼려면 `src/marry/monitor.py`의 `TARGETS` 목록을 수정하세요.

**삼성금융연수원** (하루/한 달 단위로 훑어서 확인. 시간대가 서초사옥과 다를 수 있어 고정하지 않고 화면에 보이는 시간대를 그대로 읽음)

- 2027-08-28: 그날 열려 있는 모든 시간대 확인
- 2027-09, 2027-10: 달력에 이미 "마감"으로 표시된 날짜는 건너뛰고, 마감이 아닌 날짜만 클릭해 시간대 확인

`src/marry/monitor.py`의 `HALL_DAY_SCANS`(특정 하루 전체 시간대), `HALL_MONTH_SCANS`(한 달 전체 스캔)로 관리합니다. 홀 선택이나 특정 스캔이 실패해도 나머지 홀/스캔 결과는 정상적으로 알림에 포함됩니다.

> 월 전체 스캔은 서초사옥의 4개 고정 타깃만 확인하던 것보다 실행 시간이 더 걸립니다. 실제 소요 시간을 본 뒤 GitHub Actions 분 사용량이 빠듯하면 체크 주기 조정이 필요할 수 있습니다.

## GitHub Secrets

저장소의 `Settings → Secrets and variables → Actions → New repository secret`에서 다음 네 값을 등록합니다.

- `SAMSUNG_WEDDING_EMPLOYEE_ID`: 삼성 웨딩 로그인 사원번호
- `SAMSUNG_WEDDING_EMPLOYEE_PASSWORD`: 삼성 웨딩 로그인 비밀번호
- `TELEGRAM_BOT_TOKEN`: BotFather가 발급한 봇 토큰
- `TELEGRAM_CHAT_ID`: 알림을 받을 개인 채팅 ID

비밀번호와 토큰을 파일, 커밋, 이슈 또는 Actions 로그에 입력하지 마세요.

## 최초 실행

1. 텔레그램에서 BotFather로 봇을 만들고 해당 봇에게 `/start`를 보냅니다.
2. 위 네 Secrets를 등록합니다.
3. 저장소의 `Actions → Wedding seat monitor → Run workflow`를 누릅니다.
4. 실패하면 실행 화면의 `Artifacts`에서 진단 스크린샷을 확인합니다.

보안키패드나 추가 인증 때문에 자동 로그인이 차단되면 첫 실행 로그와 스크린샷을 기준으로 로그인 선택자를 조정해야 합니다.

## 알림 정책

- 평소(전부 예약마감, 문제없음)에는 알림을 보내지 않음 - 조용히 상태만 `state/availability.json`에 기록
- 새로 `예약마감 → 예약가능`으로 바뀐 항목이 있으면 "🎉 신규!" 표시와 함께 `[삼성 웨딩 취소표 발견]` 알림
- 상태 문구를 읽지 못하면(사이트 개편 등으로 판정이 깨졌을 수 있음) `[삼성 웨딩 모니터] 상태 확인불가 ⚠️` 알림
- 오류 알림은 직전 실행과 오류 내용이 다를 때 보냄. 같은 오류가 반복되면 생략
- 오류 알림에는 최근 진단 로그와 GitHub Actions 실행 링크가 함께 담겨, 로그를 따로 열지 않아도 원인을 좁힐 수 있음
- 최근 상태는 `state/availability.json`에 날짜/시간대별로 기록 (시각은 KST 기준)
